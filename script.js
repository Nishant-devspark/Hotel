/* For clickable buttons or links */
button, a, .clickable {
    cursor: pointer;
  }
  
  /* For text inputs or editable content */
  input, textarea, [contenteditable="true"] {
    cursor: text;
  }
  
  /* For disabled elements */
  .disabled {
    cursor: not-allowed;
  }
  
  /* For draggable items */
  .draggable {
    cursor: grab;
  }
  
  .draggable:active {
    cursor: grabbing;
  }
  